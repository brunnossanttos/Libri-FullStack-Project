run:
  timeout: 3m

linters:
  enable:
    - govet
    - staticcheck
    - gofmt
    - goimports
    - revive # style checks
    - unused
    - errcheck

linters-settings:
  goimports:
    local-prefixes: github.com/brunnossanttos/libri
  gofmt:
    simplify: true
